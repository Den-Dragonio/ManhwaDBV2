{
  "name": "manhwa-app",
  "version": "1.0.0",
  "description": "IMDb/Letterboxd for Manhwa",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "firebase": "^10.14.1"
  }
}
